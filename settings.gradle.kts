plugins {
    // Allows Gradle to auto-download JDK toolchains (e.g. JDK 20) when not installed locally
    id("org.gradle.toolchains.foojay-resolver-convention") version "0.8.0"
}

rootProject.name = "chatbot-with-knowledge"
